all: ctmconv

OPENCTMDIR = ../src
OPENCTMLIB = $(OPENCTMDIR)/libopenctm.a

ctmconv: ctmconv.cpp ply.cpp ply.h $(OPENCTMLIB)
	g++ -O2 -W -I$(OPENCTMDIR) -o $@ -L$(OPENCTMDIR) ctmconv.cpp ply.cpp -lopenctm

$(OPENCTMLIB):
	cd ../src && $(MAKE) -f Makefile.macosx && cd ../tools
