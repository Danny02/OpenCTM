all: ctmconv

OPENCTMDIR = ..
OPENCTMLIB = $(OPENCTMDIR)/libopenctm.a

ctmconv: ctmconv.cpp ply.cpp ply.h $(OPENCTMLIB)
	g++ -s -O2 -W -I$(OPENCTMDIR) -o $@ -L$(OPENCTMDIR) ctmconv.cpp ply.cpp -lopenctm

$(OPENCTMLIB):
	cd .. && $(MAKE) -f Makefile.linux && cd tools
